package com.example.thepokemonapp.ui.fragments


import androidx.fragment.app.Fragment
import com.example.thepokemonapp.R

class SearchFragment : Fragment(R.layout.fragment_search) {

    //The onViewCreated() method is currently empty, so no custom behavior is added when the view is created.
    //
    //Essentially, this fragment just displays the layout without adding any logic or interactivity at the moment.
}
